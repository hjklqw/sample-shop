// Since this is a fake site, there is no difference between a dev and prod environment.
// Otherwise, IDs will differ based on the env.
// const isDev = process.env.NODE_ENV === 'development'

export const specialVariantIds = {
  quantity: 'quantity',
}

export const newArrivalsCollectionId = '675646724bd0d4618312f251'
